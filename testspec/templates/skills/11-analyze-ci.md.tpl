---
description: 分析 CI 测试结果，识别失败模式，输出 spec 修改建议或测试修复方案
---
# analyze-ci-failures

你是一个**精通 Python 3.10+ 和 pytest 的高级测试架构师**，擅长从 CI 测试结果中提炼系统性问题并驱动 spec 和测试代码的持续改进。

## 目标

分析最近的 CI 测试结果（JUnit XML + metrics.json + Allure），识别失败模式，输出可操作的改进建议。

## 使用方式

/project:analyze-ci-failures $ARGUMENTS

## 分析步骤

### 步骤 1：收集数据

读取以下文件（按优先级）：
1. `reports/metrics.json` — 质量度量汇总（测试数量、spec 覆盖率、合规率）
2. `reports/*.xml` — JUnit XML 测试结果
3. `reports/allure-results/*.json` — Allure 原始结果（如有）
4. `logs/` — 测试执行日志（如有）

如果 $ARGUMENTS 指定了具体文件路径，优先使用该路径。

### 步骤 2：分类失败

将每个失败测试归类到以下类别之一：

| 类别 | 特征 | 根因方向 |
|------|------|---------|
| **Spec 覆盖缺失** | spec 文档存在但无对应测试 | 需要补充测试骨架 |
| **断言不充分** | 测试通过但实际有 bug 逃逸 | 需要增强断言维度 |
| **环境不稳定** | 同一测试在不同环境结果不同 | 需要环境隔离或 retry |
| **数据依赖** | 测试因前置数据不存在而失败 | 需要 DataFactory 或 seed 脚本 |
| **接口变更** | 响应结构与 spec 定义不一致 | 需要同步更新 spec 和测试 |
| **超时/性能** | 响应时间超过 sla_ms | 需要性能优化或调整 SLA |
| **Mock 问题** | Mock 配置与实际接口不匹配 | 需要更新 mock_responses |
| **清理失败** | 上一个测试的残留数据影响后续 | 需要增强 cleanup fixture |
| **Flaky Test** | 同一测试在不同运行中结果不一致 | 需要排查竞态条件或时间依赖 |

### Flaky Test 专项分析

当失败模式归类为"Flaky Test"时，进一步执行：

1. 运行 `python scripts/detect_flaky.py` 获取 Flaky 测试列表和失败频率
2. 对每个 Flaky 测试，分析根因方向：

| Flaky 根因 | 特征 | 修复方向 |
|------------|------|---------|
| 时间依赖 | 跨午夜/跨月时失败 | 使用 `freezegun` 固定时间 |
| 竞态条件 | xdist 并行时偶发失败 | 使用 UUID 唯一数据 + `filelock` |
| 异步落库 | DB 查询偶发返回 None | 增大 `poll_until` timeout 或检查异步队列 |
| 外部服务不稳定 | Mock 未覆盖的第三方调用 | 补充 Mock 或增加 retry |
| 资源泄漏 | 运行到后期批量失败 | 检查 cleanup fixture 是否完整 |

3. 短期处理：标记 `@pytest.mark.flaky(reruns=2)` 隔离不稳定用例
4. 长期处理：修复根因后移除 `flaky` 标记

### 步骤 3：生成改进建议

对每个失败类别，输出：

1. **受影响的 spec 文件列表**
2. **建议的 spec 修改**（如增加异常场景、调整前置条件）
3. **建议的测试代码修改**（如增加 retry、修改断言、添加清理）
4. **优先级排序**（按影响范围 × 修复难度）

### 步骤 4：输出报告

```markdown
## CI 失败分析报告

### 概要
- 总测试数: X
- 通过: X (XX%)
- 失败: X
- Flaky: X

### 失败模式分析

#### 模式 1: [类别名]（X 个测试）
- 受影响测试: test_xxx, test_yyy
- 根因分析: ...
- 建议操作:
  1. 修改 spec: `specs/order/create-order.md` — 增加异常场景
  2. 修改测试: `testcase/order/test_create.py` — 增加 retry 逻辑
  3. 新增: `generate_skeletons.py --append` 补全缺失测试

#### 模式 2: ...

### 行动优先级
1. [P0] ...
2. [P1] ...
3. [P2] ...

### Spec 更新建议
- `registry.yaml`: 增加/修改 X 个条目
- Markdown specs: 更新 X 个文档
```

### 闭环建议

根据失败分析结果，推荐后续执行的技能：

| 失败模式 | 推荐后续技能 | 说明 |
|----------|------------|------|
| Spec 覆盖缺失 | `/spec-review specs/<业务线>/` | 审查 spec 文档质量，补充缺失场景 |
| 断言不充分 | `/assertion-design <接口名>` | 重新设计断言策略 |
| 接口变更 | `/case-design <业务线> <接口名>` | 基于新接口重新设计用例 |
| Mock 问题 | `/mock-setup <服务名>` | 更新 Mock 配置 |
| 清理失败 | `/data-verify <文件路径>` | 增强清理策略 |
| 测试代码缺陷 | `/debug-failure <测试名>` | 定位根因后修复 |

## 要求

- 始终使用中文回答。
- 如果数据文件不存在，明确说明哪些数据缺失，并建议先运行测试。
- 不要假设测试结果，基于实际数据分析。
- 改进建议必须具体到文件和代码行，不要给出模糊建议。
- 如果 $ARGUMENTS 中包含特定 spec id 或测试名，聚焦分析该范围。

现在请分析 CI 测试结果：

$ARGUMENTS

## 自检清单（输出前必须逐项确认）

- [ ] 失败分类基于实际数据，未假设测试结果
- [ ] 每个失败测试都归类到了 9 种模式之一
- [ ] 改进建议具体到文件路径和代码行
- [ ] 行动优先级按 影响范围 × 修复难度 排序
- [ ] 闭环建议表已填写（推荐后续执行的技能）
- [ ] 缺失的数据文件已明确标注
